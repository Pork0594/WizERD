# Todo Items

## Fixes

## Features
- [ ] Ship a homebrew version
- [ ] PNG output option or other common image formats.
- [ ] Proper sql arrow option. Using the correct arrow end to mean the proper sql fk that is happening.
- [ ] Multiple outputs for example having the ability to build two diagrams one with and one without labels at the same time. This could extend to other batching as well just implement some sort of batch system.
- [ ] Visual polish - add shadows, gradients, and glow effects to themes. Each theme should have its own distinctive visual style. (See comprehensive plan in docs/theme-visual-polish-plan.md)
- [ ] Handle complex config options. Give users all knobs to play with in the config file so they can fully tailor the diagram to fit their needs. Surface only the important ones to the cli so anything that is likely to be tailored can be messed with without creating a config file.
